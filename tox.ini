# content of: tox.ini , put in same dir as setup.py
[tox]
isolated_build = true
envlist = 37,38,39,310

[testenv]
allowlist_externals = poetry
commands =
    poetry install --extras yaml
    poetry run pytest
